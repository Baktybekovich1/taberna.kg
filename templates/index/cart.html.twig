{% extends 'base.html.twig' %}

{% block body %}

    {# <div class="container px-4 py-5" id="featured-3"> #}
    {#    <h2 class="pb-2 border-bottom">Товары</h2> #}
    {#    <div class="row g-4 py-5 row-cols-1 row-cols-lg-3"> #}
    <h2>Товаров: {{ count }}</h2>
    <div class="text text-end ">
        <h5>Общая стоимость: {{ price }}</h5> <a href="/" class="btn btn-outline-danger ">Оплатить</a></div>
    {% for product in  products %}
        {#            <div class="feature col"> #}

        {#                <img src="/uploads/{{ product.img }}" style="width: 250px;height: 250px"> #}

        {#                <h2>{{ product.name }}</h2> #}
        {#                <comment>{{ product.comment }}</comment> #}
        {#                <h5>{{ product.maker }}</h5> #}
        {#                <h6>{{ product.price }}сом</h6> #}
        {#                <a href="{{ url('cart_add', {id: product.id}) }}">Add more</a><br> #}
        {#                <a href="{{ url('product_more', { id: product.id }) }}" class="icon-link"> #}
        {#                    Подробнее.. #}
        {#                </a> #}
        {#            </div> #}
        <div class="list-group">
            <a href="{{ url('product_more',{id: product.id}) }}"
               class="list-group-item list-group-item-action d-flex gap-3 py-3" aria-current="true">
                <img src="/uploads/{{ product.img }}" alt="twbs" width="32" height="32"
                     class="rounded-circle flex-shrink-0">
                <div class="d-flex gap-2 w-100 justify-content-between">
                    <div>
                        <h6 class="mb-0">{{ product.name }}</h6>
                        <p class="mb-0 opacity-75">{{ product.comment }}</p>
                        <p class="mb-0 opacity-80">{{ product.maker }}</p>
                        <p class="mb-0 opacity-75">{{ product.price }}kgz</p>

                    </div>
                    <small class="opacity-50 text-nowrap"> Xшт</small>
                </div>
            </a>
        </div>


        {#            #}{#- --------------------------------------------------------------------------------------------------------------------------------------------- -#}
        {#            <div class="feature col"> #}
        {#                <div  style="width: 20rem;"> #}
        {#                    <img src="/uploads/{{ product.img }}" class="card-img-top" > #}
        {#                    <div class="card-body"> #}
        {#                        <h5 class="card-title">{{ product.name }}</h5> #}
        {#                        <p class="card-text">{{ product.comment }}</p> #}
        {#                        <a href="{{ url('product_more',{id: product.id}) }}" class="btn btn-primary">More</a> #}
        {#                    </div> #}
        {#                </div> #}
        {#            </div> #}
    {% endfor %}

{% endblock %}
{% block title %}
    Cart
{% endblock %}